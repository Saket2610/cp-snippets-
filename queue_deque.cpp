void solve(){ 
    deque<int> q;
    int x = q.back();
    q.pop_back();
    q.push_back(i)
    q.pop_front();
    q.push_front(i);

  //////
    queue<int> q;
    q.push(10);
    cout<<q.back();
    cout<<q.front();
    q.pop();
    q.size();
    q[0]; // refers to the first element in q
}
